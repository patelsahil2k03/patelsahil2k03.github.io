# Microsoft Clarity (https://clarity.microsoft.com → Project → Settings → Overview)
# Copy to .env.local for local testing. For production, set the same variable as a
# GitHub Actions secret so it is available during `npm run build` (static export).
NEXT_PUBLIC_CLARITY_PROJECT_ID=
